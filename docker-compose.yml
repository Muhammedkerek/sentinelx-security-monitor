version: "3.8"

services:

  backend:
    build: .
    container_name: sentinelx_backend
    ports:
      - "3000:3000"
    env_file:
      - .env
    depends_on:
      - redis

  redis:
    image: redis:7
    container_name: sentinelx_redis
    ports:
      - "6379:6379"